export interface RedisDriverDetails {
  driverId: string;
  distance: number;
  rating: number;
  cancelCount: number;
  score: number;
  vehicleModel: string;
  vehicleNumber: string;
  driverName: string;
  driverPhoto: string;
  phoneNumber: string;
  stripeId: string;
  stripeLinkUrl:string;
}